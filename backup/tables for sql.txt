artworks:
'identifier': string,
'title': string,
'artist': string,
'year': string,
'theoreticalPrice': int,
'actualPrice': int, 
'hidden': boolean
'currentOwner': string (already existing student username)

create table artworks 
(identifier VARCHAR(), 
title VARCHAR(), 
artist VARCHAR()
year YEAR,
theoreticalPrice SMALLINT(),
actualPrice SMALLINT(),
hidden BOOL,
currentOwner VARCHAR())

history:
'identifier', string
'seller', string
'buyer', string
'price', int
'timestamp', date object? is that a thing in SQL? i forget

create table history 
(identifier VARCHAR(), 
seller VARCHAR(), 
buyer VARCHAR()
price SMALLINT(),
timestamp DATETIME())

students:
'username': string,
'name': string,
'admin': boolean,
'guilders': integer,
'microresearchPoints': integer

create table history 
(username VARCHAR(), 
name VARCHAR(), 
admin BOOL,
guilders SMALLINT(),
microresearchPoints SMALLINT())

'microresearch':
'username': string (already existing student username),
'identifier': string (already existing artwork identifier)
'information': string
'timestamp': date object? is that a thing in SQL? i forget

create table microresearch 
(username VARCHAR(), 
identifier VARCHAR(), 
information TEXT,
timestamp DATETIME)
